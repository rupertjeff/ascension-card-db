default:
    autoload:
        '': %paths.base%/tests/behat/bootstrap
    extensions:
        Laracasts\Behat:
            #env_path: .env.behat
        Behat\MinkExtension:
            default_session: laravel
            laravel: ~
    suites:
        features:
            paths:    [ %paths.base%/tests/behat/api ]
            contexts: [ ApiContext ]
